{
  # Program configurations (programs.*)
  # These files configure programs using home-manager's programs.* options
  # For package installation only, use packages.nix instead
  imports = [
    ./git.nix
  ];
}

